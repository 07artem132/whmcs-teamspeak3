#Это ядро серверного модуля TeamSpeak 3 
Реализовано расширение поиска по таким критериям как:
* uid
* port
* server hostname:port

В панели управления услуги (администратора) реализован вывод: 
* port
* uid
* server hostname:port

